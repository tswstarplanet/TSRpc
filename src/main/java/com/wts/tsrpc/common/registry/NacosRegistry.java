package com.wts.tsrpc.common.registry;

import com.wts.tsrpc.server.manage.Application;

public class NacosRegistry implements Registry {
    @Override
    public void register(Application application, RegisterConfig config) {

    }

    @Override
    public void deregister(Application application, RegisterConfig config) {

    }
}
